# 项目规则

## 环境配置
- **Python 环境：必须使用 conda 的 `common` 环境运行项目**
- **禁止使用系统 Python 环境**

## 项目技术栈
- 后端：Flask (Python)
- 数据库：SQLite (exam_system.db)
- 前端：原生 JavaScript + HTML/CSS

## 常用命令
- 运行自动化测试：`./run_tests.sh`（自动激活环境并执行完整测试流程）
